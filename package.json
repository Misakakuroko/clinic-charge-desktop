{
  "name": "clinic-charge-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Windows 单机离线门诊收费录入、查询与打印软件",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/domain.mjs && node --check src/app.mjs && node --check electron/main.cjs && node --check electron/preload.cjs && npm test",
    "package:win": "electron-packager . ClinicCharge --platform=win32 --arch=x64 --out=release --overwrite --asar --prune=true --ignore='^/release$' --ignore='^/tests$' --win32metadata.ProductName='门诊收费录入打印软件' --win32metadata.FileDescription='门诊收费录入打印软件'"
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "electron": "44.1.1"
  }
}
