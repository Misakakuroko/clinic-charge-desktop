{
  "name": "clinic-charge-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "Windows 单机离线门诊收费录入、查询与打印软件",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/domain.mjs && node --check src/app.mjs && node --check electron/main.cjs && node --check electron/preload.cjs && node --check electron/storage.cjs && node --check scripts/windows-smoke.cjs && npm test",
    "package:win": "node scripts/package-windows.mjs",
    "test:windows": "node scripts/windows-smoke.cjs"
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "electron": "44.1.1"
  }
}
